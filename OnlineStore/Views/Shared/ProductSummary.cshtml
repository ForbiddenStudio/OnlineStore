@model OnlineStoreDomain.ProductEntity

        <!-- Это для вставки картинки
        <div class="col-lg-6">
            <img src="~/Images/Lim.jpg"/>
        </div>-->
<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("# руб")</span>
    </h3>
    
    @using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.Id)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Добавить в корзину" />
        </div>
    }
    <span class="lead">Объем: @Model.Volume л</span>
</div>
@*<h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("# руб")</span>
    </h3>*@

@*<span class="lead">@Model.Description</span>  отображение описания*@
<div class="col-lg-1">
</div>
